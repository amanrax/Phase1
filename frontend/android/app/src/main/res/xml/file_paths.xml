<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Existing paths (keep these) -->
    <external-path name="my_images" path="." />
    <cache-path name="my_cache_images" path="." />
    
    <!-- Additional paths for comprehensive file access -->
    
    <!-- Internal app files directory -->
    <files-path name="files" path="." />
    <files-path name="app_files" path="/" />
    
    <!-- Cache directories -->
    <cache-path name="cache" path="." />
    <cache-path name="app_cache" path="/" />
    
    <!-- External storage root -->
    <external-path name="external" path="." />
    <external-path name="external_root" path="/" />
    
    <!-- External cache -->
    <external-cache-path name="external_cache" path="." />
    <external-cache-path name="ext_cache" path="/" />
    
    <!-- External files (app-specific) -->
    <external-files-path name="external_files" path="." />
    <external-files-path name="ext_files" path="/" />
    
    <!-- Documents folder -->
    <external-files-path name="documents" path="Documents/" />
    <external-files-path name="docs" path="Documents" />
    
    <!-- Downloads folder -->
    <external-files-path name="downloads" path="Download/" />
    <external-files-path name="download" path="Download" />
    
    <!-- Pictures folder (for farmer photos) -->
    <external-files-path name="pictures" path="Pictures/" />
    <external-files-path name="images" path="Pictures" />
    
    <!-- PDF storage -->
    <external-files-path name="pdfs" path="PDFs/" />
    
    <!-- ID Cards specific folder -->
    <external-files-path name="id_cards" path="IDCards/" />
    
    <!-- Media storage -->
    <external-files-path name="media" path="Media/" />
</paths>
