# Development environment
# For local testing on PC (when backend runs on same machine)
VITE_API_BASE_URL=http://localhost:8000

# For testing mobile app while backend runs on PC
# Uncomment and use your PC IP when testing on physical device
# VITE_MOBILE_API_URL=http://192.168.1.66:8000
